# AGENTS.md

This repository uses Codex as a coding agent.

## Local conventions
- Keep changes minimal and focused on the request.
- Prefer updating existing tests over adding new ones.
- When adding new tests, keep them deterministic and mock external services.

## Running tests
- Use the project’s standard test command (see `package.json` scripts).

## Notes
- If additional project-specific instructions are needed, add them here.

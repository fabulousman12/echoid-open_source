<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color (optional, change as needed) -->
    <item android:drawable="@color/cardview_light_background" />


    <!-- Logo image with padding to reduce visible size -->
    <item
        android:top="100dp"
        android:left="60dp"
        android:right="60dp"
        android:bottom="100dp">
        <bitmap
            android:gravity="center"
            android:src="@drawable/compresslog2" />
    </item>

</layer-list>
